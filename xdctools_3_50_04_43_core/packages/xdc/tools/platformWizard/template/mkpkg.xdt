%%{

    var OPTIONS = [
        "repositoryPath",
        "schemaFile",
        "platformSchema"
    ];
    
    this.pkgName = this.arguments[0];
    
    var opts = this.arguments[1];

    for (var i = 0; i < OPTIONS.length; i++) {
        this[OPTIONS[i]] = opts[OPTIONS[i]];
    }
    
    
%%}


package.bld.xdt -> package.bld
package.xdc.xdt -> package.xdc
config.bld.xdt -> config.bld
Platform.xdc.xdt -> Platform.xdc
Platform.xs.xdt -> Platform.xs



